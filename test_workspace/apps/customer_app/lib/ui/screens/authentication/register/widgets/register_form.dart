// Generated By XFlutter Cli.
//
// more info: https://xflutter-cli.com
import 'package:customer_app/router/app_router.dart';
import 'package:flutter/material.dart';
import 'package:reactive_forms/reactive_forms.dart';
import 'package:easy_localization/easy_localization.dart';
import 'package:core/core.dart';
import '../viewmodels/register_viewmodel.dart';

class RegisterForm extends StatelessWidget {
  const RegisterForm({super.key});

  @override
  Widget build(BuildContext context) {
    return InstanceBuilder<RegisterViewModel>(
      builder: (viewModel) => ReactiveForm(
        formGroup: viewModel.params.registerForm,
        child: Column(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Column(
              children: [
                CustomizedReactiveFormField(
                  formControlName: 'name',
                  labelText: 'name'.tr(),
                  keyboardType: TextInputType.text,
                  textInputAction: TextInputAction.next,
                  onSubmitted: (control) => viewModel.params.registerForm.focus('email'),
                ),
                const SizedBox(height: formSpacing),
                CustomizedReactiveFormField(
                  formControlName: 'email',
                  labelText: 'email'.tr(),
                  keyboardType: TextInputType.emailAddress,
                  textInputAction: TextInputAction.next,
                  onSubmitted: (control) => viewModel.params.registerForm.focus('password'),
                ),
                const SizedBox(height: formSpacing),
                CustomizedReactiveFormField(
                  formControlName: 'password',
                  labelText: 'password'.tr(),
                  keyboardType: TextInputType.text,
                  textInputAction: TextInputAction.next,
                  obscureText: true,
                  onSubmitted: (control) => viewModel.params.registerForm.focus('confirm_password'),
                ),
                const SizedBox(height: formSpacing),
                CustomizedReactiveFormField(
                  formControlName: 'confirm_password',
                  labelText: 'confirm_password'.tr(),
                  keyboardType: TextInputType.text,
                  textInputAction: TextInputAction.done,
                  obscureText: true,
                  onSubmitted: (control) {},
                ),
                const SizedBox(height: formSpacing),
              ],
            ),
            Column(
              children: [
                ReactiveFormConsumer(
                  builder: (context, form, child) => SizedBox(
                    width: double.infinity,
                    child: CustomizedButton(
                      child: Text('next'.tr()),
                      callback: () {
                        if (form.valid) {
                          viewModel.submit();
                        } else {
                          form.markAllAsTouched();
                        }
                      },
                    ),
                  ),
                ),
                Row(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Text("already_have_account".tr(), style: smallTextStyle),
                    AppTextButton(
                      text: "login".tr(),
                      textStyle: smallTextStyle,
                      callback: () => appRouter.maybePop(),
                    ),
                  ],
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
