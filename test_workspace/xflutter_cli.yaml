# workspace name
workspace: test_workspace

# xflutter_cli version when you initialize your application,
#
# If you upgrade the version of the tool and find major changes between the latest version and the version that initializes your application,
# you can downgrade version manually by run command:
# dart pub global activate xflutter_cli 2.1.7
version: 2.1.7

# project modules
modules:
  # module environment
  # Don't modify the env values, changes may throw exceptions when you run any command
  - env:
      # only `mvvm` architecture is supported now
      architecture: mvvm
      
      # module template (`app` or `package`)
      template: app
      
      # the tool will generate and modify all components which located in this directory 
      # `main.dart` path for `app` template
      # `{package_path}/src` for `package` template
      base: apps/booking_app/lib/
      
      # path of `pubspec.yaml`
      path: apps/booking_app/
      
      # project name (same of name in `pubspec.yaml`)
      name: booking_app
      
      # machine Flutter version when initialize the module
      flutter_version: 3.22.2

    # xflutter_cli generate configuration
    generate:
      # avoid generate files
      # skip:
        # - ui/widgets/result/result_builder.dart
        # - models/data/result/result.dart
        # - models/responses/list_response/list_response.dart
        
      # run `dart run build_runner build --delete-conflicting-outputs`
      # after generate screen, model or crud
      run_build_runner: true

      # overwrite file if exists
      overwrite: false
    
  - env:
      architecture: mvvm
      template: package
      base: packages/utilities/lib/src/
      path: packages/utilities/
      name: utilities
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/extensions/lib/src/
      path: packages/extensions/
      name: extensions
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/events/lib/src/
      path: packages/events/
      name: events
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/storage/lib/src/
      path: packages/storage/
      name: storage
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/models/lib/src/
      path: packages/models/
      name: models
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/theme/lib/src/
      path: packages/theme/
      name: theme
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/widgets/lib/src/
      path: packages/widgets/
      name: widgets
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: package
      base: packages/state_management/lib/src/
      path: packages/state_management/
      name: state_management
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
  - env:
      architecture: mvvm
      template: app
      base: apps/admin_app/lib/
      path: apps/admin_app/
      name: admin_app
      flutter_version: 3.22.2
    generate:
      run_build_runner: true
      overwrite: false
