// Generated By XFlutter Cli.
//
// more info: https://xflutter-cli.com
import 'package:customer_app/common/router/app_router.dart';
import 'package:flutter/material.dart';
import 'package:reactive_forms/reactive_forms.dart';
import 'package:easy_localization/easy_localization.dart';
import 'package:core/core.dart';
import '../viewmodels/login_viewmodel.dart';

class LoginForm extends StatelessWidget {
  const LoginForm({super.key});

  @override
  Widget build(BuildContext context) {
    return InstanceBuilder<LoginViewModel>(
      builder: (viewModel) => ReactiveForm(
        formGroup: viewModel.params.loginForm,
        child: Column(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Column(
              children: [
                CustomizedReactiveFormField(
                  formControlName: 'email',
                  labelText: 'email'.tr(),
                  keyboardType: TextInputType.emailAddress,
                  textInputAction: TextInputAction.next,
                  onSubmitted: (control) => viewModel.params.loginForm.focus('password'),
                ),
                const SizedBox(height: formSpacing),
                CustomizedReactiveFormField(
                  formControlName: 'password',
                  labelText: 'password'.tr(),
                  keyboardType: TextInputType.text,
                  textInputAction: TextInputAction.done,
                  obscureText: true,
                  onSubmitted: (control) {},
                ),
                const SizedBox(height: formSpacing),
                Align(
                  alignment: AlignmentDirectional.centerStart,
                  child: AppTextButton(
                    text: "forget_password".tr(),
                    textStyle: smallTextStyle,
                    callback: () => appRouter.push(const ForgetPassword()),
                  ),
                ),
                const SizedBox(height: formSpacing),
              ],
            ),
            Column(
              children: [
                ReactiveFormConsumer(
                  builder: (context, form, child) => SizedBox(
                    width: double.infinity,
                    child: CustomizedButton(
                      child: Text('next'.tr()),
                      callback: () {
                        if (form.valid) {
                          viewModel.submit();
                        } else {
                          form.markAllAsTouched();
                        }
                      },
                    ),
                  ),
                ),
                Row(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Text("not_have_account".tr(), style: smallTextStyle),
                    AppTextButton(
                      text: "create_account".tr(),
                      textStyle: smallTextStyle,
                      callback: () => appRouter.push(const Register()),
                    ),
                  ],
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
