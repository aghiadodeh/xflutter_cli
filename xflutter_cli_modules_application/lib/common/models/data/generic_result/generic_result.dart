// Generated By XFlutter Cli.
//
// more info: https://xflutter-cli.com
import 'package:freezed_annotation/freezed_annotation.dart';

part 'generic_result.freezed.dart';

@freezed
sealed class Result<T> with _$Result<T> {
  /// get data value when it is [ResultIdle] or [ResultData].
  T? get data {
    if (this is ResultIdle<T>) {
      return (this as ResultIdle<T>).data;
    } else if (this is ResultData<T>) {
      return (this as ResultData<T>).data;
    }
    return null;
  }

  const Result._();

  const factory Result.idle({T? data}) = ResultIdle<T>;

  const factory Result.loading() = ResultLoading<T>;

  const factory Result.data(T data) = ResultData<T>;

  const factory Result.error(String? error) = ResultError<T>;
}
