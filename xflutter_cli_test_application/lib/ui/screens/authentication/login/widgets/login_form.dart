// Generated By XFlutter Cli.
//
// more info: https://xflutter-cli.com
import 'package:flutter/material.dart';
import 'package:easy_localization/easy_localization.dart';
import '../viewmodels/login_viewmodel.dart';
import 'package:reactive_forms/reactive_forms.dart';
import 'package:xflutter_cli_test_application/ui/resources/dimensions/dimensions.dart';
import 'package:xflutter_cli_test_application/ui/widgets/buttons/customized_button.dart';
import 'package:xflutter_cli_test_application/ui/widgets/instance/instance_builder.dart';
import 'package:xflutter_cli_test_application/ui/widgets/text_fields/customized_reactive_phone_field.dart';

class LoginForm extends StatelessWidget {
  const LoginForm({super.key});

  @override
  Widget build(BuildContext context) {
    return InstanceBuilder<LoginViewModel>(
      builder: (LoginViewModel viewModel) {
        final formGroup = viewModel.params.loginForm;

        return ReactiveForm(
          formGroup: formGroup,
          child: Column(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              // form fields
              Column(
                children: [
                  // phone
                  CustomizedReactivePhoneField(
                    formControlName: 'phone',
                    labelText: 'phone'.tr(),
                    textInputAction: TextInputAction.done,
                    onSubmitted: () {},
                  ),
                  const SizedBox(height: formSpacing),
                ],
              ),

              // submit widget
              SizedBox(
                width: double.infinity,
                child: CustomizedButton(
                  child: Text('next'.tr()),
                  callback: () {
                    if (formGroup.valid) {
                      viewModel.submit();
                    } else {
                      formGroup.markAllAsTouched();
                    }
                  },
                ),
              ),
            ],
          ),
        );
      },
    );
  }
}
