// Generated By XFlutter Cli.
//
// more info: https://xflutter-cli.com
import 'package:flutter/material.dart';
import 'package:xflutter_cli_test_application/models/models.dart';
import 'package:xflutter_cli_test_application/ui/widgets/core/theme_widget.dart';

class ProductCardWidget extends StatelessWidget {
  final Product product;
  const ProductCardWidget(this.product, {super.key});

  @override
  Widget build(BuildContext context) {
    final media = product.media?.firstOrNull;
    if (media?.originalUrl != null) {
      precacheImage(NetworkImage(media!.originalUrl!), context);
    }
    return ThemeWidget(
      builder: (BuildContext context, ThemeData theme) => Container(
        margin: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
        padding: const EdgeInsets.all(10),
        child: Column(
          children: [
            Row(
              children: [
                if (media?.originalUrl != null)
                  Image.network(
                    media!.originalUrl!,
                    fit: BoxFit.cover,
                    width: 50,
                    height: 50,
                  ),
                Flexible(
                  child: Text(product.name ?? ""),
                ),
              ],
            ),
            const Divider()
          ],
        ),
      ),
    );
  }
}
